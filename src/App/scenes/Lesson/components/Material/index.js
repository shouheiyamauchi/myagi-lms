import React from 'react';
import PropTypes from 'prop-types';

const Material = props => {
  const {
  } = props;

  return (
    <div>Material</div>
  );
}

Material.propTypes = {
}

export default Material;
